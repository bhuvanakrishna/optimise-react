import React from 'react';
import { Select as AntMultiSelect } from 'antd';

type Props = any;

const MultiSelect = (props: Props) => {
  return <AntMultiSelect {...props} />;
};

export default MultiSelect;
